// src/components/live/StreamPreview.js

import React from 'react';

const StreamPreview = () => {
  return (
    <div className="stream-preview">
      {/* Contenu de l'aperçu du stream */}
      <h2>Stream Preview</h2>
      <p>Placeholder for stream preview content.</p>
    </div>
  );
};

export default StreamPreview;
